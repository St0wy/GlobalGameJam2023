using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace GlobalGameJam
{
    public class SeedBehaviour : MonoBehaviour
    {
        [SerializeField] private float _seedSpeed = 5.0f;
            
        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
